require('dotenv').config(); // https://www.npmjs.com/package/dotenv

(async () => {
  console.log('yo.');
})();

// PLAN

// Read from Evernote API

// Write to compiler's file storage

// Post to slackbot
